{% block content %}
    <h1>Pridėk naują produktų kategoriją!</h1>
    {{ form(form) }}
    <hr><br>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>Pavadinimas</th>
            <th>Foto</th>
        </tr>
        {% for entity in entities %}
            <tr>

                <td>{{ entity.id }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.photo }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}